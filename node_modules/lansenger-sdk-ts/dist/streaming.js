"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createStreamMessage = createStreamMessage;
exports.fetchStreamMessage = fetchStreamMessage;
const urlHelpers_1 = require("./urlHelpers");
const http_1 = require("./http");
const models_1 = require("./models");
async function createStreamMessage(config, appToken, receiverId, receiverType, streamId, opts) {
    if (!receiverId)
        return new models_1.StreamMessageResult({ success: false, error: "receiver_id is required" });
    if (!receiverType)
        return new models_1.StreamMessageResult({ success: false, error: "receiver_type is required" });
    if (!streamId)
        return new models_1.StreamMessageResult({ success: false, error: "stream_id is required" });
    const url = (0, urlHelpers_1.buildApiUrl)(config, "sse", "msg_create", appToken);
    const body = { receiverId, receiverType, streamId };
    const [data, httpErr] = await (0, http_1.doPost)(url, body, opts?.fetchFn);
    if (httpErr)
        return new models_1.StreamMessageResult({ success: false, error: httpErr });
    const [ok, apiErr] = (0, http_1.parseApiResponse)(data);
    if (!ok)
        return new models_1.StreamMessageResult({ success: false, error: apiErr });
    const d = data.data || {};
    return new models_1.StreamMessageResult({ success: true, message_id: d.msgId, raw_response: data });
}
async function fetchStreamMessage(config, appToken, msgId, opts) {
    if (!msgId)
        return new models_1.StreamMessageResult({ success: false, error: "msg_id is required" });
    const url = (0, urlHelpers_1.buildApiUrl)(config, "sse", "msg_fetch", appToken);
    const body = { msgId };
    const [data, httpErr] = await (0, http_1.doPost)(url, body, opts?.fetchFn);
    if (httpErr)
        return new models_1.StreamMessageResult({ success: false, error: httpErr });
    const [ok, apiErr] = (0, http_1.parseApiResponse)(data);
    if (!ok)
        return new models_1.StreamMessageResult({ success: false, error: apiErr });
    const d = data.data || {};
    return new models_1.StreamMessageResult({ success: true, message_id: d.msgId, raw_response: data });
}
//# sourceMappingURL=streaming.js.map