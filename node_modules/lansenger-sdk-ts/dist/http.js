"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.doGet = doGet;
exports.doPost = doPost;
exports.doPostMultipart = doPostMultipart;
exports.parseApiResponse = parseApiResponse;
async function doGet(url, fetchFn) {
    const fn = fetchFn || fetch;
    try {
        const response = await fn(url);
        if (!response.ok) {
            return [null, `HTTP error: ${response.status} ${response.statusText}`];
        }
        const data = await response.json();
        return [data, null];
    }
    catch (e) {
        const msg = e instanceof Error ? e.message : String(e);
        return [null, `Request error: ${msg}`];
    }
}
async function doPost(url, body, fetchFn) {
    const fn = fetchFn || fetch;
    try {
        const response = await fn(url, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify(body),
        });
        if (!response.ok) {
            return [null, `HTTP error: ${response.status} ${response.statusText}`];
        }
        const data = await response.json();
        return [data, null];
    }
    catch (e) {
        const msg = e instanceof Error ? e.message : String(e);
        return [null, `Request error: ${msg}`];
    }
}
async function doPostMultipart(url, formData, fetchFn) {
    const fn = fetchFn || fetch;
    try {
        const response = await fn(url, {
            method: "POST",
            body: formData,
        });
        if (!response.ok) {
            return [null, `HTTP error: ${response.status} ${response.statusText}`];
        }
        const data = await response.json();
        return [data, null];
    }
    catch (e) {
        const msg = e instanceof Error ? e.message : String(e);
        return [null, `Request error: ${msg}`];
    }
}
function parseApiResponse(data) {
    const errCode = data.errCode ?? -1;
    if (errCode !== 0) {
        const msg = data.errMsg || "Unknown error";
        return [false, `API error (errCode=${errCode}): ${msg}`];
    }
    return [true, null];
}
//# sourceMappingURL=http.js.map