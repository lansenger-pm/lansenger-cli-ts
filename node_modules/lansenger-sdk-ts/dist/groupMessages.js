"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.sendGroupMessage = sendGroupMessage;
const urlHelpers_1 = require("./urlHelpers");
const http_1 = require("./http");
const models_1 = require("./models");
async function sendGroupMessage(config, appToken, groupId, msgType, msgData, opts) {
    if (!msgType)
        return new models_1.SendMessageResult({ success: false, error: "msg_type is required" });
    if (!groupId)
        return new models_1.SendMessageResult({ success: false, error: "group_id is required" });
    if (!msgData)
        return new models_1.SendMessageResult({ success: false, error: "msg_data is required" });
    const userToken = opts?.user_token || "";
    const url = (0, urlHelpers_1.buildApiUrl)(config, "smart_bot", "group_message", appToken, { userToken });
    const payload = { groupId, msgType, msgData };
    if (opts?.sender_id)
        payload.senderId = opts.sender_id;
    if (opts?.outlines)
        payload.outlines = opts.outlines;
    if (opts?.uuid)
        payload.uuid = opts.uuid;
    if (opts?.entry_id)
        payload.entryId = opts.entry_id;
    const [data, httpErr] = await (0, http_1.doPost)(url, payload, opts?.fetchFn);
    if (httpErr)
        return new models_1.SendMessageResult({ success: false, error: httpErr, retryable: true });
    const errCode = data.errCode ?? -1;
    if (errCode !== 0) {
        const msg = data.errMsg || "Unknown error";
        return new models_1.SendMessageResult({ success: false, error: `API error (errCode=${errCode}): ${msg}`, retryable: true });
    }
    const d = data.data || {};
    return new models_1.SendMessageResult({
        success: true, message_id: d.msgId, msg_type: msgType,
        operation: "group_message", raw_response: data,
    });
}
//# sourceMappingURL=groupMessages.js.map