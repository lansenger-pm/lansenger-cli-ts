import { LansengerConfig } from "./config";
import { FetchFn } from "./http";
import { StreamMessageResult } from "./models";
export declare function createStreamMessage(config: LansengerConfig, appToken: string, receiverId: string, receiverType: string, streamId: string, opts?: {
    fetchFn?: FetchFn;
}): Promise<StreamMessageResult>;
export declare function fetchStreamMessage(config: LansengerConfig, appToken: string, msgId: string, opts?: {
    fetchFn?: FetchFn;
}): Promise<StreamMessageResult>;
