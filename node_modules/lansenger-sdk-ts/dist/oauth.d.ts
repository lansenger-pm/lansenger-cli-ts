import { LansengerConfig } from "./config";
import { FetchFn } from "./http";
import { UserTokenResult } from "./models";
export declare function buildAuthorizeUrl(config: LansengerConfig, redirectUri: string, opts?: {
    scope?: string | string[];
    state?: string;
}): string;
export declare function exchangeCodeForUserToken(config: LansengerConfig, appToken: string, code: string, opts?: {
    redirect_uri?: string;
    fetchFn?: FetchFn;
}): Promise<UserTokenResult>;
export declare function refreshUserToken(config: LansengerConfig, appToken: string, refreshToken: string, opts?: {
    scope?: string;
    fetchFn?: FetchFn;
}): Promise<UserTokenResult>;
export declare function parseAuthorizeCallback(queryString: string | Record<string, string>): Record<string, string>;
export declare function validateCallbackState(callbackState: string, expectedState: string): boolean;
