"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ChatMessagesResult = exports.ChatMessageInfo = exports.ChatListResult = exports.ChatGroupInfo = exports.ChatStaffInfo = exports.ScheduleAttendeeMetaResult = exports.ScheduleUpdateResult = exports.ScheduleAttendeesResult = exports.ScheduleListResult = exports.ScheduleInfoResult = exports.ScheduleCreateResult = exports.CalendarPrimaryResult = exports.TodoTaskExecutorListResult = exports.TodoTaskStatusCountResult = exports.TodoTaskListResult = exports.TodoTaskInfoResult = exports.TodoTaskCreateResult = exports.DepartmentStaffsResult = exports.DepartmentChildrenResult = exports.DepartmentDetailResult = exports.IsInGroupResult = exports.GroupListResult = exports.UpdateGroupMembersResult = exports.UpdateGroupResult = exports.GroupMemberResult = exports.GroupInfoResult = exports.CreateGroupResult = exports.GroupCreateInfo = exports.StreamMessageResult = exports.BotMessageResult = exports.UserMessageResult = exports.AccountMessageResult = exports.UserInfoResult = exports.UserTokenResult = exports.DynamicCardUpdateParams = exports.OaCardParams = exports.LinkCardParams = exports.AppCardParams = exports.MediaPathResult = exports.DownloadMediaResult = exports.UploadMediaResult = exports.QueryGroupsResult = exports.StaffSearchResult = exports.ExtraFieldIdsResult = exports.OrgInfoResult = exports.StaffIdMappingResult = exports.DepartmentAncestorsResult = exports.StaffDetailResult = exports.StaffBasicInfoResult = exports.SendMessageResult = void 0;
class SendMessageResult {
    constructor(init) {
        this.success = init.success;
        this.message_id = init.message_id ?? null;
        this.error = init.error ?? null;
        this.platform = init.platform ?? "lansenger";
        this.msg_type = init.msg_type ?? null;
        this.operation = init.operation ?? null;
        this.raw_response = init.raw_response ?? null;
        this.retryable = init.retryable ?? false;
    }
    toDict() {
        const d = { success: this.success, platform: this.platform };
        if (this.message_id !== null)
            d.message_id = this.message_id;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.SendMessageResult = SendMessageResult;
class StaffBasicInfoResult {
    constructor(init) {
        this.success = init.success;
        this.org_id = init.org_id ?? null;
        this.org_name = init.org_name ?? null;
        this.name = init.name ?? null;
        this.gender = init.gender ?? null;
        this.signature = init.signature ?? null;
        this.avatar_url = init.avatar_url ?? null;
        this.avatar_id = init.avatar_id ?? null;
        this.status = init.status ?? null;
        this.departments = init.departments ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        for (const key of ["org_id", "org_name", "name", "gender", "signature", "avatar_url", "avatar_id", "status", "departments"]) {
            const v = this[key];
            if (v !== null)
                d[key] = v;
        }
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.StaffBasicInfoResult = StaffBasicInfoResult;
class StaffDetailResult {
    constructor(init) {
        this.success = init.success;
        const keys = ["name", "signature", "avatar_id", "avatar_url", "status", "departments", "gender", "org_id", "org_name", "login_name", "employee_number", "email", "external_id", "nationality", "birthdate", "id_number", "native_place", "duties", "parties", "address", "mobile_phone", "extra_phones", "introduction", "education", "career", "login_ways", "tags", "extra_field_set", "leaders", "join_date", "error", "raw_response"];
        for (const k of keys) {
            this[k] = init[k] ?? null;
        }
    }
    toDict() {
        const d = { success: this.success };
        for (const key of ["name", "signature", "avatar_id", "avatar_url", "status", "departments", "gender", "org_id", "org_name", "login_name", "employee_number", "email", "external_id", "nationality", "birthdate", "id_number", "native_place", "duties", "parties", "address", "mobile_phone", "extra_phones", "introduction", "education", "career", "login_ways", "tags", "extra_field_set", "leaders", "join_date"]) {
            const v = this[key];
            if (v !== null)
                d[key] = v;
        }
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.StaffDetailResult = StaffDetailResult;
class DepartmentAncestorsResult {
    constructor(init) {
        this.success = init.success;
        this.ancestor_groups = init.ancestor_groups ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.ancestor_groups !== null)
            d.ancestor_groups = this.ancestor_groups;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.DepartmentAncestorsResult = DepartmentAncestorsResult;
class StaffIdMappingResult {
    constructor(init) {
        this.success = init.success;
        this.staff_id = init.staff_id ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.staff_id !== null)
            d.staff_id = this.staff_id;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.StaffIdMappingResult = StaffIdMappingResult;
class OrgInfoResult {
    constructor(init) {
        this.success = init.success;
        this.org_id = init.org_id ?? null;
        this.org_name = init.org_name ?? null;
        this.icon_url = init.icon_url ?? null;
        this.org_max_member_limit = init.org_max_member_limit ?? null;
        this.org_order_type = init.org_order_type ?? null;
        this.org_days_limit = init.org_days_limit ?? null;
        this.org_billing_date = init.org_billing_date ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        for (const key of ["org_id", "org_name", "icon_url", "org_max_member_limit", "org_order_type", "org_days_limit", "org_billing_date"]) {
            const v = this[key];
            if (v !== null)
                d[key] = v;
        }
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.OrgInfoResult = OrgInfoResult;
class ExtraFieldIdsResult {
    constructor(init) {
        this.success = init.success;
        this.has_more = init.has_more ?? false;
        this.total = init.total ?? 0;
        this.extra_field_ids = init.extra_field_ids ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success, has_more: this.has_more, total: this.total };
        if (this.extra_field_ids !== null)
            d.extra_field_ids = this.extra_field_ids;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.ExtraFieldIdsResult = ExtraFieldIdsResult;
class StaffSearchResult {
    constructor(init) {
        this.success = init.success;
        this.has_more = init.has_more ?? false;
        this.total = init.total ?? 0;
        this.staff_info = init.staff_info ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success, has_more: this.has_more, total: this.total };
        if (this.staff_info !== null)
            d.staff_info = this.staff_info;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.StaffSearchResult = StaffSearchResult;
class QueryGroupsResult {
    constructor(init) {
        this.success = init.success;
        this.total_group_ids = init.total_group_ids ?? 0;
        this.group_ids = init.group_ids ?? [];
        this.error = init.error ?? null;
        this.platform = init.platform ?? "lansenger";
        this.operation = init.operation ?? "query_groups";
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = {
            success: this.success,
            total_group_ids: this.total_group_ids,
            group_ids: this.group_ids,
            platform: this.platform,
            operation: this.operation,
        };
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.QueryGroupsResult = QueryGroupsResult;
class UploadMediaResult {
    constructor(init) {
        this.success = init.success;
        this.media_id = init.media_id ?? null;
        this.created_time = init.created_time ?? null;
        this.raw_response = init.raw_response ?? null;
        this.error = init.error ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.media_id !== null)
            d.media_id = this.media_id;
        if (this.created_time !== null)
            d.created_time = this.created_time;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.UploadMediaResult = UploadMediaResult;
class DownloadMediaResult {
    constructor(init) {
        this.success = init.success;
        this.data = init.data ?? null;
        this.error = init.error ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.data !== null)
            d.size = this.data.length;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.DownloadMediaResult = DownloadMediaResult;
class MediaPathResult {
    constructor(init) {
        this.success = init.success;
        this.media_path = init.media_path ?? null;
        this.name = init.name ?? null;
        this.type = init.type ?? null;
        this.size = init.size ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        for (const key of ["media_path", "name", "type", "size"]) {
            const v = this[key];
            if (v !== null)
                d[key] = v;
        }
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.MediaPathResult = MediaPathResult;
class AppCardParams {
    constructor(init) {
        this.body_title = init.body_title;
        this.chat_id = init.chat_id ?? "";
        this.head_title = init.head_title ?? "";
        this.body_sub_title = init.body_sub_title ?? "";
        this.body_content = init.body_content ?? "";
        this.signature = init.signature ?? "";
        this.fields = init.fields ?? null;
        this.links = init.links ?? null;
        this.card_link = init.card_link ?? "";
        this.pc_card_link = init.pc_card_link ?? "";
        this.pad_card_link = init.pad_card_link ?? "";
        this.is_dynamic = init.is_dynamic ?? false;
        this.head_status_info = init.head_status_info ?? null;
        this.staff_id = init.staff_id ?? "";
        this.head_icon_url = init.head_icon_url ?? "";
        this.is_group = init.is_group ?? false;
        this.user_token = init.user_token ?? "";
        this.sender_id = init.sender_id ?? "";
    }
}
exports.AppCardParams = AppCardParams;
class LinkCardParams {
    constructor(init) {
        this.chat_id = init.chat_id ?? "";
        this.title = init.title ?? "";
        this.link = init.link ?? "";
        this.description = init.description ?? "";
        this.icon_link = init.icon_link ?? "";
        this.pc_link = init.pc_link ?? "";
        this.pad_link = init.pad_link ?? "";
        this.from_name = init.from_name ?? "";
        this.from_icon_link = init.from_icon_link ?? "";
        this.is_group = init.is_group ?? false;
        this.user_token = init.user_token ?? "";
        this.sender_id = init.sender_id ?? "";
    }
}
exports.LinkCardParams = LinkCardParams;
class OaCardParams {
    constructor(init) {
        this.chat_id = init.chat_id ?? "";
        this.head = init.head ?? "";
        this.title = init.title ?? "";
        this.sub_title = init.sub_title ?? "";
        this.staff_id = init.staff_id ?? "";
        this.fields = init.fields ?? null;
        this.link = init.link ?? "";
        this.pc_link = init.pc_link ?? "";
        this.pad_link = init.pad_link ?? "";
        this.card_action = init.card_action ?? null;
        this.is_group = init.is_group ?? false;
        this.user_token = init.user_token ?? "";
        this.sender_id = init.sender_id ?? "";
    }
}
exports.OaCardParams = OaCardParams;
class DynamicCardUpdateParams {
    constructor(init) {
        this.msg_id = init.msg_id;
        this.head_status_info = init.head_status_info ?? null;
        this.links = init.links ?? null;
        this.is_last_update = init.is_last_update ?? false;
    }
}
exports.DynamicCardUpdateParams = DynamicCardUpdateParams;
class UserTokenResult {
    constructor(init) {
        this.success = init.success;
        this.user_token = init.user_token ?? null;
        this.expires_in = init.expires_in ?? 7200;
        this.refresh_token = init.refresh_token ?? null;
        this.refresh_expires_in = init.refresh_expires_in ?? 2592000;
        this.staff_id = init.staff_id ?? null;
        this.scope = init.scope ?? null;
        this.state = init.state ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.user_token !== null) {
            d.user_token = this.user_token;
            d.expires_in = this.expires_in;
        }
        if (this.refresh_token !== null) {
            d.refresh_token = this.refresh_token;
            d.refresh_expires_in = this.refresh_expires_in;
        }
        if (this.staff_id !== null)
            d.staff_id = this.staff_id;
        if (this.scope !== null)
            d.scope = this.scope;
        if (this.state !== null)
            d.state = this.state;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.UserTokenResult = UserTokenResult;
class UserInfoResult {
    constructor(init) {
        this.success = init.success;
        this.staff_id = init.staff_id ?? null;
        this.name = init.name ?? null;
        this.org_id = init.org_id ?? null;
        this.org_name = init.org_name ?? null;
        this.avatar_id = init.avatar_id ?? null;
        this.avatar_url = init.avatar_url ?? null;
        this.mobile_phone = init.mobile_phone ?? null;
        this.email = init.email ?? null;
        this.employee_number = init.employee_number ?? null;
        this.login_name = init.login_name ?? null;
        this.external_id = init.external_id ?? null;
        this.department = init.department ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.staff_id !== null)
            d.staff_id = this.staff_id;
        if (this.name !== null)
            d.name = this.name;
        if (this.org_id !== null)
            d.org_id = this.org_id;
        if (this.org_name !== null)
            d.org_name = this.org_name;
        if (this.avatar_id !== null)
            d.avatar_id = this.avatar_id;
        if (this.avatar_url !== null)
            d.avatar_url = this.avatar_url;
        if (this.email !== null)
            d.email = this.email;
        if (this.employee_number !== null)
            d.employee_number = this.employee_number;
        if (this.login_name !== null)
            d.login_name = this.login_name;
        if (this.external_id !== null)
            d.external_id = this.external_id;
        if (this.mobile_phone !== null)
            d.mobile_phone = this.mobile_phone;
        if (this.department !== null)
            d.department = this.department;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.UserInfoResult = UserInfoResult;
class AccountMessageResult {
    constructor(init) {
        this.success = init.success;
        this.message_id = init.message_id ?? null;
        this.invalid_staff = init.invalid_staff ?? null;
        this.invalid_department = init.invalid_department ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.message_id !== null)
            d.message_id = this.message_id;
        if (this.invalid_staff !== null)
            d.invalid_staff = this.invalid_staff;
        if (this.invalid_department !== null)
            d.invalid_department = this.invalid_department;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.AccountMessageResult = AccountMessageResult;
class UserMessageResult {
    constructor(init) {
        this.success = init.success;
        this.message_id = init.message_id ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.message_id !== null)
            d.message_id = this.message_id;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.UserMessageResult = UserMessageResult;
class BotMessageResult {
    constructor(init) {
        this.success = init.success;
        this.message_id = init.message_id ?? null;
        this.invalid_staff = init.invalid_staff ?? null;
        this.invalid_department = init.invalid_department ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.message_id !== null)
            d.message_id = this.message_id;
        if (this.invalid_staff !== null)
            d.invalid_staff = this.invalid_staff;
        if (this.invalid_department !== null)
            d.invalid_department = this.invalid_department;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.BotMessageResult = BotMessageResult;
class StreamMessageResult {
    constructor(init) {
        this.success = init.success;
        this.message_id = init.message_id ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.message_id !== null)
            d.message_id = this.message_id;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.StreamMessageResult = StreamMessageResult;
class GroupCreateInfo {
    constructor(init) {
        this.name = init.name;
        this.org_id = init.org_id;
        this.owner_id = init.owner_id ?? "";
        this.description = init.description ?? "";
        this.avatar_id = init.avatar_id ?? "";
        this.staff_id_list = init.staff_id_list ?? null;
        this.department_id_list = init.department_id_list ?? null;
        this.apply_request_id = init.apply_request_id ?? "";
        this.apply_notes = init.apply_notes ?? "";
        this.apply_global_unique_id = init.apply_global_unique_id ?? "";
        this.apply_session_unique_id = init.apply_session_unique_id ?? "";
    }
}
exports.GroupCreateInfo = GroupCreateInfo;
class CreateGroupResult {
    constructor(init) {
        this.success = init.success;
        this.group_id = init.group_id ?? null;
        this.total_members = init.total_members ?? 0;
        this.invalid_staff = init.invalid_staff ?? null;
        this.invalid_department = init.invalid_department ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.group_id !== null)
            d.group_id = this.group_id;
        d.total_members = this.total_members;
        if (this.invalid_staff !== null)
            d.invalid_staff = this.invalid_staff;
        if (this.invalid_department !== null)
            d.invalid_department = this.invalid_department;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.CreateGroupResult = CreateGroupResult;
class GroupInfoResult {
    constructor(init) {
        this.success = init.success;
        const keys = ["name", "description", "avatar_id", "avatar_url", "owner", "creator", "state", "manage_mode", "location_share", "needs_confirm", "is_public", "max_members", "max_history_msg_count", "total_members", "remind_all", "send_msg_status", "error", "raw_response"];
        for (const k of keys) {
            this[k] = init[k] ?? null;
        }
    }
    toDict() {
        const d = { success: this.success };
        for (const key of ["name", "description", "avatar_id", "avatar_url", "owner", "creator", "state", "manage_mode", "location_share", "needs_confirm", "is_public", "max_members", "max_history_msg_count", "total_members", "remind_all", "send_msg_status"]) {
            const v = this[key];
            if (v !== null)
                d[key] = v;
        }
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.GroupInfoResult = GroupInfoResult;
class GroupMemberResult {
    constructor(init) {
        this.success = init.success;
        this.total_members = init.total_members ?? 0;
        this.members = init.members ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success, total_members: this.total_members };
        if (this.members !== null)
            d.members = this.members;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.GroupMemberResult = GroupMemberResult;
class UpdateGroupResult {
    constructor(init) {
        this.success = init.success;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.UpdateGroupResult = UpdateGroupResult;
class UpdateGroupMembersResult {
    constructor(init) {
        this.success = init.success;
        this.total_members = init.total_members ?? 0;
        this.added_staff_count = init.added_staff_count ?? 0;
        this.deleted_staff_count = init.deleted_staff_count ?? 0;
        this.invalid_staff = init.invalid_staff ?? null;
        this.invalid_department = init.invalid_department ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success, total_members: this.total_members, added_staff_count: this.added_staff_count, deleted_staff_count: this.deleted_staff_count };
        if (this.invalid_staff !== null)
            d.invalid_staff = this.invalid_staff;
        if (this.invalid_department !== null)
            d.invalid_department = this.invalid_department;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.UpdateGroupMembersResult = UpdateGroupMembersResult;
class GroupListResult {
    constructor(init) {
        this.success = init.success;
        this.total_group_ids = init.total_group_ids ?? 0;
        this.group_ids = init.group_ids ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success, total_group_ids: this.total_group_ids };
        if (this.group_ids !== null)
            d.group_ids = this.group_ids;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.GroupListResult = GroupListResult;
class IsInGroupResult {
    constructor(init) {
        this.success = init.success;
        this.is_in_group = init.is_in_group ?? false;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success, is_in_group: this.is_in_group };
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.IsInGroupResult = IsInGroupResult;
class DepartmentDetailResult {
    constructor(init) {
        this.success = init.success;
        const keys = ["id", "name", "external_id", "parent_id", "order", "has_children", "normal_members", "inactive_members", "frozen_members", "deleted_members", "tags", "ancestor_departments", "leaders", "emails", "phones", "addresses", "introductions", "dept_type", "error", "raw_response"];
        for (const k of keys) {
            this[k] = init[k] ?? null;
        }
    }
    toDict() {
        const d = { success: this.success };
        for (const key of ["id", "name", "external_id", "parent_id", "order", "has_children", "normal_members", "inactive_members", "frozen_members", "deleted_members", "tags", "ancestor_departments", "leaders", "emails", "phones", "addresses", "introductions", "dept_type"]) {
            const v = this[key];
            if (v !== null)
                d[key] = v;
        }
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.DepartmentDetailResult = DepartmentDetailResult;
class DepartmentChildrenResult {
    constructor(init) {
        this.success = init.success;
        this.departments = init.departments ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.departments !== null)
            d.departments = this.departments;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.DepartmentChildrenResult = DepartmentChildrenResult;
class DepartmentStaffsResult {
    constructor(init) {
        this.success = init.success;
        this.has_more = init.has_more ?? false;
        this.total = init.total ?? 0;
        this.staffs = init.staffs ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success, has_more: this.has_more, total: this.total };
        if (this.staffs !== null)
            d.staffs = this.staffs;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.DepartmentStaffsResult = DepartmentStaffsResult;
class TodoTaskCreateResult {
    constructor(init) {
        this.success = init.success;
        this.todotask_id = init.todotask_id ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.todotask_id !== null)
            d.todotask_id = this.todotask_id;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.TodoTaskCreateResult = TodoTaskCreateResult;
class TodoTaskInfoResult {
    constructor(init) {
        this.success = init.success;
        const keys = ["todotask_id", "source_id", "title", "desc", "status", "type", "link", "pc_link", "sender_id", "executor_ids", "create_time", "app_id", "error", "raw_response"];
        for (const k of keys) {
            this[k] = init[k] ?? null;
        }
    }
    toDict() {
        const d = { success: this.success };
        for (const key of ["todotask_id", "source_id", "title", "desc", "status", "type", "link", "pc_link", "sender_id", "executor_ids", "create_time", "app_id"]) {
            const v = this[key];
            if (v !== null)
                d[key] = v;
        }
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.TodoTaskInfoResult = TodoTaskInfoResult;
class TodoTaskListResult {
    constructor(init) {
        this.success = init.success;
        this.total = init.total ?? 0;
        this.todotask_list = init.todotask_list ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success, total: this.total };
        if (this.todotask_list !== null)
            d.todotask_list = this.todotask_list;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.TodoTaskListResult = TodoTaskListResult;
class TodoTaskStatusCountResult {
    constructor(init) {
        this.success = init.success;
        this.status_counts = init.status_counts ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.status_counts !== null)
            d.status_counts = this.status_counts;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.TodoTaskStatusCountResult = TodoTaskStatusCountResult;
class TodoTaskExecutorListResult {
    constructor(init) {
        this.success = init.success;
        this.total = init.total ?? 0;
        this.executor_list = init.executor_list ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success, total: this.total };
        if (this.executor_list !== null)
            d.executor_list = this.executor_list;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.TodoTaskExecutorListResult = TodoTaskExecutorListResult;
class CalendarPrimaryResult {
    constructor(init) {
        this.success = init.success;
        const keys = ["calendar_id", "summary", "description", "permissions", "color", "type", "role", "error", "raw_response"];
        for (const k of keys) {
            this[k] = init[k] ?? null;
        }
    }
    toDict() {
        const d = { success: this.success };
        for (const key of ["calendar_id", "summary", "description", "permissions", "color", "type", "role"]) {
            const v = this[key];
            if (v !== null)
                d[key] = v;
        }
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.CalendarPrimaryResult = CalendarPrimaryResult;
class ScheduleCreateResult {
    constructor(init) {
        this.success = init.success;
        this.schedule_id = init.schedule_id ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.schedule_id !== null)
            d.schedule_id = this.schedule_id;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.ScheduleCreateResult = ScheduleCreateResult;
class ScheduleInfoResult {
    constructor(init) {
        this.success = init.success;
        const keys = ["schedule_id", "summary", "description", "repeat_type", "all_day", "start_time", "end_time", "creator", "rsvp_status", "error", "raw_response"];
        for (const k of keys) {
            this[k] = init[k] ?? null;
        }
    }
    toDict() {
        const d = { success: this.success };
        for (const key of ["schedule_id", "summary", "description", "repeat_type", "all_day", "start_time", "end_time", "creator", "rsvp_status"]) {
            const v = this[key];
            if (v !== null)
                d[key] = v;
        }
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.ScheduleInfoResult = ScheduleInfoResult;
class ScheduleListResult {
    constructor(init) {
        this.success = init.success;
        this.schedule_list = init.schedule_list ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.schedule_list !== null)
            d.schedule_list = this.schedule_list;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.ScheduleListResult = ScheduleListResult;
class ScheduleAttendeesResult {
    constructor(init) {
        this.success = init.success;
        this.total = init.total ?? 0;
        this.attendees = init.attendees ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success, total: this.total };
        if (this.attendees !== null)
            d.attendees = this.attendees;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.ScheduleAttendeesResult = ScheduleAttendeesResult;
class ScheduleUpdateResult {
    constructor(init) {
        this.success = init.success;
        this.schedule_ids = init.schedule_ids ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.schedule_ids !== null)
            d.schedule_ids = this.schedule_ids;
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.ScheduleUpdateResult = ScheduleUpdateResult;
class ScheduleAttendeeMetaResult {
    constructor(init) {
        this.success = init.success;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.ScheduleAttendeeMetaResult = ScheduleAttendeeMetaResult;
class ChatStaffInfo {
    constructor(init) {
        this.staff_id = init.staff_id ?? "";
        this.staff_name = init.staff_name ?? "";
        this.sector_names = init.sector_names ?? null;
    }
    toDict() {
        const d = { staff_id: this.staff_id, staff_name: this.staff_name };
        if (this.sector_names !== null)
            d.sector_names = this.sector_names;
        return d;
    }
}
exports.ChatStaffInfo = ChatStaffInfo;
class ChatGroupInfo {
    constructor(init) {
        this.group_id = init.group_id ?? "";
        this.group_name = init.group_name ?? "";
    }
    toDict() {
        return { group_id: this.group_id, group_name: this.group_name };
    }
}
exports.ChatGroupInfo = ChatGroupInfo;
class ChatListResult {
    constructor(init) {
        this.success = init.success;
        this.staff_infos = init.staff_infos ?? null;
        this.group_infos = init.group_infos ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success };
        if (this.staff_infos !== null)
            d.staff_infos = this.staff_infos.map(s => s.toDict());
        if (this.group_infos !== null)
            d.group_infos = this.group_infos.map(g => g.toDict());
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.ChatListResult = ChatListResult;
class ChatMessageInfo {
    constructor(init) {
        this.send_time = init.send_time ?? "";
        this.sender = init.sender ?? "";
        this.message_type = init.message_type ?? "";
        this.content = init.content ?? null;
    }
    toDict() {
        const d = { send_time: this.send_time, sender: this.sender, message_type: this.message_type };
        if (this.content !== null)
            d.content = this.content;
        return d;
    }
}
exports.ChatMessageInfo = ChatMessageInfo;
class ChatMessagesResult {
    constructor(init) {
        this.success = init.success;
        this.has_more = init.has_more ?? false;
        this.total = init.total ?? 0;
        this.last_version = init.last_version ?? "";
        this.name = init.name ?? "";
        this.chat_type = init.chat_type ?? "";
        this.messages = init.messages ?? null;
        this.error = init.error ?? null;
        this.raw_response = init.raw_response ?? null;
    }
    toDict() {
        const d = { success: this.success, has_more: this.has_more, total: this.total, last_version: this.last_version, name: this.name, chat_type: this.chat_type };
        if (this.messages !== null)
            d.messages = this.messages.map(m => m.toDict());
        if (this.error !== null)
            d.error = this.error;
        return d;
    }
}
exports.ChatMessagesResult = ChatMessagesResult;
//# sourceMappingURL=models.js.map